{
  "name": "reputablechat",
  "private": true,
  "description": "Browser tests only. The application ships no JavaScript dependencies: its two vendored files are committed with their hashes recorded in CLAUDE.md.",
  "scripts": {
    "browser-spec": "node spec/browser/account.mjs"
  },
  "devDependencies": {
    "playwright-core": "^1.49.0"
  },
  "//": "No \"type\": \"module\" here. It silences a cosmetic Node warning and breaks script/derive_key.mjs, which loads the vendored Argon2 UMD build through createRequire -- under type:module that .js file is treated as ESM and the CommonJS export disappears."
}
